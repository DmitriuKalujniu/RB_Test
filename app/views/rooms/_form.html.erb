<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :user_id, value: current_user.id, autofocus: true, required: true, class:'form-control' %>

  <div class="field">
    <%= form.label :name , class:'form-label'%><br />
    <%= form.text_field :name, autofocus: true, required: true, class:'form-control' %>
  </div>

  <div class="field">
    <%= form.label :link, class:'form-label' %><br />
    <%= form.url_field :link, required: true, class:'form-control' %>
  </div>


  <div>
    <%= form.submit class: 'btn btn-primary mt-3' %>

  </div>
<% end %>
